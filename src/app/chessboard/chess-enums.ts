export enum Colour {
    black,
    white
}

export enum Piece {
    king,
    queen,
    rook,
    bishop,
    knight,
    pawn
}

let files = ["a", "b", "c", "d", "e", "f", "g", "h"];
export { files };